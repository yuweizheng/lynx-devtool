[
  {
    "name": "App_callStaticVoidMethod",
    "description": "Invoke a static void method in the host application using reflection.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "className": {
          "type": "string",
          "description": "The full path name of the class to reflect"
        },
        "methodName": {
          "type": "string",
          "description": "The name of the static method to call"
        }
      },
      "required": [
        "clientId",
        "className",
        "methodName"
      ]
    }
  },
  {
    "name": "App_getBOE",
    "description": "Get the BOE lane information about the client.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "App_getPPE",
    "description": "Get the PPE lane information about the client.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "App_setBOE",
    "description": "Set the BOE lane of the client.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "enable": {
          "type": "boolean",
          "description": "Whether to enable BOE."
        },
        "lane": {
          "type": "string",
          "description": "The BOE lane to be enabled"
        }
      },
      "required": [
        "clientId",
        "enable",
        "lane"
      ]
    }
  },
  {
    "name": "App_setPPE",
    "description": "Set the PPE lane of the client.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "enable": {
          "type": "boolean",
          "description": "Whether to enable PPE."
        },
        "lane": {
          "type": "string",
          "description": "The PPE lane to be enabled"
        }
      },
      "required": [
        "clientId",
        "enable",
        "lane"
      ]
    }
  },
  {
    "name": "App_startAnyMock",
    "description": "Start the AnyMock (任意门) on the client",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "nodeId"
      ]
    }
  },
  {
    "name": "App_stopAnyMock",
    "description": "Stop the AnyMock (任意门) on the client",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "CSS_getBackgroundColors",
    "description": "Returns background color information for the node.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "CSS_getComputedStyleForNode",
    "description": "Returns the computed style for a DOM node identified by nodeId.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "CSS_getInlineStylesForNode",
    "description": "Returns inline style and style attribute for the node.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "CSS_getMatchedStylesForNode",
    "description": "Returns CSS rules matching the specified node. The matchedCSSRules in the result are ordered by priority from high to low. When selectors have the same specificity, rules that appear later (further down) have higher priority.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "CSS_getStyleSheetText",
    "description": "Returns the text content of the stylesheet.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "styleSheetId": {
          "type": "string",
          "description": "Style sheet identifier"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "styleSheetId"
      ]
    }
  },
  {
    "name": "Debugger_getScriptSource",
    "description": "Get the source code of a script identified by the given script identifier. Use `saveToTmp` if the response is too large.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "scriptId": {
          "type": "string",
          "description": "Identifier of the script. Use `Debugger_listScripts` to get the script IDs of a session."
        },
        "saveToTmp": {
          "type": "boolean",
          "default": false,
          "description": "Whether to save the script source to a temporary file if it is large."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "scriptId"
      ]
    }
  },
  {
    "name": "Debugger_listScripts",
    "description": "List all parsed scripts. If no scripts found, it means that the page is opened before the DevTool connected. Use `Page_reload` to reload the page and get the scripts again.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "Device_closePage",
    "description": "Close the current page",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "Device_listAvailableApps",
    "description": "List all available apps on all devices.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "deviceId": {
          "type": "string",
          "description": "The deviceId. Use the `Device_listDevices` (if available, otherwise use `Device_acquireDevice`) to get ID for a devices."
        }
      },
      "required": [
        "deviceId"
      ]
    }
  },
  {
    "name": "Device_listClients",
    "description": "List all connected clients. This tool may timeout if no clients are connected or just started. Use `Devices_reconnect` and retry in a few seconds if that happens.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "Device_listDevices",
    "description": "List all connected devices.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "Device_listSessions",
    "description": "List all opened sessions",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "Device_openApp",
    "description": "Open an app on the device.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "deviceId": {
          "type": "string",
          "description": "The deviceId. Use the `Device_listDevices` (if available, otherwise use `Device_acquireDevice`) to get ID for a devices."
        },
        "packageName": {
          "type": "string",
          "description": "The package name of the app. Get it from the `packageName` field of response of `Device_listAvailableApps`."
        },
        "withDataCleared": {
          "type": "boolean",
          "description": "Whether to clear app data before opening the app."
        }
      },
      "required": [
        "deviceId",
        "packageName"
      ]
    }
  },
  {
    "name": "Device_openPage",
    "description": "Open a page",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "url": {
          "type": "string",
          "description": "The URL of the page"
        }
      },
      "required": [
        "clientId",
        "url"
      ]
    }
  },
  {
    "name": "DOM_getAttributes",
    "description": "Get all attributes of the specified node.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "DOM_getBoxModel",
    "description": "Get the box model of an element.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "DOM_getDocument",
    "description": "Get the document tree of the page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "DOM_getDocumentWithBoxModel",
    "description": "Get the document tree of the Lynx page with box model information.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "DOM_getNodeForLocation",
    "description": "Get the node at the specified coordinates.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "x": {
          "type": "number",
          "description": "X coordinate"
        },
        "y": {
          "type": "number",
          "description": "Y coordinate"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "x",
        "y"
      ]
    }
  },
  {
    "name": "DOM_getOriginalNodeIndex",
    "description": "Get the original index of the node in its parent.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "DOM_getSearchResults",
    "description": "Get search results for the specified range.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "searchId": {
          "type": "number",
          "description": "Search identifier"
        },
        "fromIndex": {
          "type": "number",
          "description": "Start index for search results"
        },
        "toIndex": {
          "type": "number",
          "description": "End index for search results"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "searchId",
        "fromIndex",
        "toIndex"
      ]
    }
  },
  {
    "name": "DOM_innerText",
    "description": "Get the visible text content of the node.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "DOM_performSearch",
    "description": "Search for nodes in the DOM tree.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "query": {
          "type": "string",
          "description": "Search query string"
        },
        "includeUserAgentShadowDOM": {
          "type": "boolean",
          "description": "Whether to include user agent shadow DOM in search"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "query"
      ]
    }
  },
  {
    "name": "DOM_pushNodesByBackendIdsToFrontend",
    "description": "Push backend node IDs to the frontend for inspection.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "backendNodeIds": {
          "type": "array",
          "description": "Array of backend node IDs",
          "items": {
            "type": "number"
          }
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "backendNodeIds"
      ]
    }
  },
  {
    "name": "DOM_querySelector",
    "description": "Find the first element matching the CSS selector.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier. Optional; defaults to root node if omitted."
        },
        "selector": {
          "type": "string",
          "description": "CSS selector string"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "selector"
      ]
    }
  },
  {
    "name": "DOM_querySelectorAll",
    "description": "Find all elements matching the CSS selector.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier. Optional; defaults to root node if omitted."
        },
        "selector": {
          "type": "string",
          "description": "CSS selector string"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "selector"
      ]
    }
  },
  {
    "name": "DOM_requestChildNodes",
    "description": "Request child nodes for a given parent node.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        },
        "depth": {
          "type": "number",
          "description": "Depth of child nodes to retrieve"
        },
        "pierce": {
          "type": "boolean",
          "description": "Whether to pierce through shadow DOM"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "DOM_scrollIntoViewIfNeeded",
    "description": "Scrolls the specified rect of the given node into view if not already visible.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "nodeId": {
          "type": "number",
          "description": "Identifier of the node. Unique DOM node identifier."
        },
        "rect": {
          "type": "object",
          "description": "The rect to be scrolled into view, relative to the node's border box, in CSS pixels. When omitted, center of the node will be used.",
          "properties": {
            "x": { "type": "number" },
            "y": { "type": "number" },
            "width": { "type": "number" },
            "height": { "type": "number" }
          }
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "nodeId"
      ]
    }
  },
  {
    "name": "Input_emulateTouchFromMouseEvent",
    "description": "Emulate touch from mouse event - converts mouse events to touch events for testing touch interactions",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "x": {
          "type": "number",
          "description": "X coordinate"
        },
        "y": {
          "type": "number",
          "description": "Y coordinate"
        },
        "type": {
          "type": "string",
          "enum": ["mousePressed", "mouseReleased", "mouseMoved"],
          "description": "Type of mouse event"
        },
        "timestamp": {
          "type": "number",
          "description": "Timestamp of the mouse event"
        },
        "button": {
          "type": "string",
          "enum": ["left", "middle", "right"],
          "description": "Mouse button"
        },
        "deltaX": {
          "type": "number",
          "description": "Horizontal scroll delta (optional)"
        },
        "deltaY": {
          "type": "number",
          "description": "Vertical scroll delta (optional)"
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "type",
        "x",
        "y",
        "timestamp",
        "button"
      ]
    }
  },
  {
    "name": "JSB_getGlobalProps",
    "description": "Returns the global properties for the given session.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "JSB_listCalls",
    "description": "Returns a list of all JSB calls for the given session.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "JSB_listEvents",
    "description": "Returns a list of all JSB events for the given session.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "Page_reload",
    "description": "Reload the current page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "url": {
          "type": "string",
          "description": "The URL to reload, if different from the current page. Optional."
        },
        "ignoreCache": {
          "type": "boolean",
          "default": true,
          "description": "Whether to ignore the cache when reloading the page. Defaults to `true`"
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "Page_takeScreenshot",
    "description": "Take a screenshot of the current page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "PIA_getAllStatus",
    "description": "Get all features status(prefetch / snapshot / nsr / ssr / preload_html / preload_res etc.) of a PIA page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "statusType": {
          "type": "string",
          "enum": ["prefetch", "snapshot", "nsr", "ssr", "preload_html", "preload_res"],
          "description": "One of the status types: prefetch / snapshot / nsr / ssr / preload_html / preload_res."
        }
      },
      "required": [
        "clientId",
        "sessionId",
        "statusType"
      ]
    }
  },
  {
    "name": "PIA_getAllTiming",
    "description": "Get all metric time durations(FR3 / PPE / FCP ..etc) of a PIA page",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "PIA_getGlobalProps",
    "description": "Get global props of a PIA page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "PIA_getManifestInfo",
    "description": "Get manifest info of a PIA page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "PIA_getOriginalUrl",
    "description": "Get original URL of a PIA page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  },
  {
    "name": "PIA_invokeJSB",
    "description": "Invoke JSB in current page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "methodName": {
          "type": "string",
          "description": "The name of the JSB method to invoke."
        },
        "methodInput": {
          "type": "string",
          "description": "The input of the JSB method, JSON string."
        }
      },
      "required": [
        "clientId",
        "methodName",
        "methodInput"
      ]
    }
  },
  {
    "name": "PIA_listJSBRecord",
    "description": "List invoked JSB records in current page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "methodNames": {
          "type": "string"
        }
      },
      "required": [
        "clientId",
        "methodNames"
      ]
    }
  },
  {
    "name": "PIA_listPIALog",
    "description": "List client sdk logs with PIA tags in current page.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        }
      },
      "required": [
        "clientId"
      ]
    }
  },
  {
    "name": "PIA_switchForceEnableFeatures",
    "description": "Set whether to force enable all PIA features(previously named 'Disable all PIA Settings') in the global switch.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "PIA_switchForceOutMode",
    "description": "Set whether to force entry into PIA out-sdk mode in the global switch.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "PIA_switchShowPIAConsole",
    "description": "Set whether to show PIA draggable console panel on screen in the global switch.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "Runtime_listConsole",
    "description": "List all console messages.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string",
          "description": "The clientId to list sessions. Use `Device_listClients` to get the ID. If somehow no clients are found, tool `Device_openApp` (sometimes unavailable) may help."
        },
        "sessionId": {
          "type": "number",
          "description": "The sessionId to list sessions. Use `Device_listSessions` to get the ID."
        },
        "offset": {
          "type": "number",
          "description": "The number of console messages to skip before returning results."
        },
        "limit": {
          "type": "number",
          "minimum": 1,
          "maximum": 100,
          "description": "The maximum number of console messages to return."
        },
        "includeStackTraces": {
          "type": "boolean",
          "description": "By default, only error messages would contain stack traces. Set this to true to include stack traces for all messages in the output."
        },
        "level": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["log", "info", "warning", "error", "debug"]
          },
          "description": "The log level to filter messages. Defaults to ['info', 'log', 'warning', 'error']"
        }
      },
      "required": [
        "clientId",
        "sessionId"
      ]
    }
  }
]
